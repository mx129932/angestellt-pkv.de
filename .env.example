# FTP-Zugang — echte Werte in .env (NICHT commiten)
FTP_HOST=your-host.example.com
FTP_PORT=21
FTP_USER=example-user
FTP_PASSWORD=example-password
FTP_SECURE=false

# Remote-Pfade (führender / Pflicht, trailing / Pflicht)
FTP_REMOTE_TEST=/test.pkv-angestellt.de/
FTP_REMOTE_LIVE=/pkv-angestellt.de/
