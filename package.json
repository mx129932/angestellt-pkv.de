{
  "name": "pkv-angestellt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "quality": "node scripts/quality-gate.js",
    "deploy:test": "npm run build && node scripts/upload.js",
    "deploy:test:dry": "npm run build && node scripts/upload.js --dry-run",
    "deploy:test:full": "npm run build && node scripts/upload.js --full",
    "deploy:live": "npm run build && node scripts/upload.js --live",
    "deploy:live:dry": "npm run build && node scripts/upload.js --live --dry-run",
    "images": "node scripts/generate-images.js",
    "images:list": "node scripts/generate-images.js --list",
    "images:alt": "node scripts/generate-images.js --export-alt",
    "images:convert": "node scripts/generate-images.js --convert"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.1.4",
    "@fontsource/inter": "^5.1.0",
    "@fontsource/newsreader": "^5.1.0",
    "astro": "^4.16.18",
    "openai": "^6.34.0",
    "sharp": "^0.34.5",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "basic-ftp": "^5.0.5"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.60.1"
  }
}
